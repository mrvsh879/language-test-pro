import fs from 'node:fs/promises';

const path = 'app.js';
let app = await fs.readFile(path, 'utf8');

const oldRender = /function renderSpeaking\(q\)\{[\s\S]*?\n\$\('#recordBtn'\)\.addEventListener[\s\S]*?\}\)\}/;
const newRender = `function renderSpeaking(q){let box=$('#speakingBox');if(!box){box=document.createElement('div');box.id='speakingBox';box.className='speaking-box';$('#options').before(box)}if(q.skill!=='speaking'){box.classList.add('hidden');box.innerHTML='';return}box.classList.remove('hidden');const rec=state.recordings[q.id],ans=state.answers[q.id],isRecording=recorder?.state==='recording';const details=ans?.scoreDetails||{};box.innerHTML=\`<div class="speaking-head"><div><strong>\${esc(t('readAloudHint'))}</strong><span id="recordStatus">\${ans?esc(t('analysisDone')):rec?.blob?esc(t('recordReady')):isRecording?esc(t('recording')):''}</span></div><b id="recordClock">\${rec?.durationSec?fmt(rec.durationSec):'00:00'}</b></div><div class="record-actions"><button id="recordBtn" class="record-button \${isRecording?'recording':''}" type="button">\${esc(isRecording?t('stop'):t('record'))}</button><button id="analyzeBtn" class="primary" type="button" \${rec?.blob&&!ans?'':'disabled'}>\${esc(t('analyze'))}</button></div>\${rec?.url?\`<audio class="record-preview" controls preload="metadata" src="\${rec.url}"></audio>\`:''}<div id="submitStatus" class="submit-status"></div><div id="analysisResult" class="analysis-result \${ans?'':'hidden'}">\${ans?\`<strong>Общая оценка: \${ans.score}%</strong><div class="score-grid"><span>Точность текста <b>\${details.textMatch??0}%</b></span><span>Полнота <b>\${details.completeness??0}%</b></span><span>Понятность речи <b>\${details.recognitionConfidence??0}%</b></span><span>Темп <b>\${details.pace??0}%</b></span></div><p><b>Слов в минуту:</b> \${details.wordsPerMinute??'—'}</p><p><b>\${esc(t('transcript'))}:</b> \${esc(ans.transcript)}</p><small>Автоматическая оценка понятности и точности чтения. Это не сертифицированная фонетическая экспертиза.</small>\`:''}</div>\`;
$('#recordBtn').addEventListener('click',()=>{if(recorder?.state==='recording'){stopRecording()}else{startRecording(q)}});$('#analyzeBtn').addEventListener('click',()=>analyzeRecording(q))}`;

if (!oldRender.test(app)) throw new Error('renderSpeaking block not found');
app = app.replace(oldRender, newRender);

const oldAnalyze = /async function analyzeRecording\(q\)\{[\s\S]*?\nfunction renderQuestion/;
const newAnalyze = `async function analyzeRecording(q){const rec=state.recordings[q.id];if(!rec?.blob||rec.durationSec<3||rec.blob.size<1500){alert('Запись пустая или слишком короткая.');return}const btn=$('#analyzeBtn'),status=$('#submitStatus');btn.disabled=true;btn.textContent=t('analyzing');if(status)status.textContent='Загрузка записи…';const fd=new FormData();const ext=rec.blob.type.includes('mp4')?'mp4':rec.blob.type.includes('ogg')?'ogg':'webm';fd.append('audio',rec.blob,\`reading.\${ext}\`);fd.append('candidateId',state.candidate.candidateId||state.candidate.email);fd.append('sessionId',state.sessionId);fd.append('questionId',q.id);fd.append('referenceText',q.passage);fd.append('durationSec',String(rec.durationSec));fd.append('level',q.level);const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),90000);try{if(status)status.textContent='AI распознаёт и оценивает чтение…';const r=await fetch(WORKER_URL,{method:'POST',body:fd,signal:controller.signal});const raw=await r.text();let data;try{data=JSON.parse(raw)}catch{throw Error(raw||\`HTTP \${r.status}\`)}if(!r.ok||!data.success)throw Error(data.details||data.error||\`HTTP \${r.status}\`);const score=Number(data.scores?.overall??data.scores?.readingAccuracy??0);if(!data.transcript?.trim())throw Error('Речь не распознана. Запишите чтение ещё раз.');state.answers[q.id]={correct:score>=60,score,scoreDetails:data.scores||{},transcript:data.transcript,recordingKey:data.recordingKey||'',answerText:\`\${score}%\`,answeredAt:new Date().toISOString()};if(status)status.textContent='Проверка завершена.';renderSpeaking(q)}catch(e){const message=e.name==='AbortError'?'Сервер не ответил за 90 секунд. Повторите отправку.':e.message;if(status)status.textContent=\`Ошибка: \${message}\`;alert(\`\${t('analysisFailed')} \${message}\`);btn.disabled=false;btn.textContent=t('analyze')}finally{clearTimeout(timeout)}}
function renderQuestion`;

if (!oldAnalyze.test(app)) throw new Error('analyzeRecording block not found');
app = app.replace(oldAnalyze, newAnalyze);

await fs.writeFile(path, app);
console.log('Speaking submit fixed');
