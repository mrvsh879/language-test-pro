name: Generate German listening audio

on:
  workflow_dispatch:
  push:
    paths:
      - 'data/de-listening.json'
      - 'scripts/generate-de-audio.mjs'
      - '.github/workflows/generate-de-audio.yml'

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Generate missing German MP3 files
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: node scripts/generate-de-audio.mjs
      - name: Commit German listening audio
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add audio/de
          if git diff --cached --quiet; then
            echo "No new audio files"
          else
            git commit -m "Add German listening audio"
            git push
          fi
